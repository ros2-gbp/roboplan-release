namespace roboplan::example_models {
namespace anchor {
// This function exists solely as an anchor to locate the exported library on
// disk at runtime. It must be compiled as a separate unit so that it is not
// wrapped up in the python library.
void example_models_location_anchor() {}
}  // namespace anchor
}  // namespace roboplan::example_models
